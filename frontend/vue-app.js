/* Vue 3 用户云盘：不依赖构建工具，直接由 Nginx 提供静态文件。 */
const apiBase = window.STORAGE_API_BASE || "";

Vue.createApp({
  data() { return {
    token: localStorage.getItem("storage_token") || "", user: {}, groups: [], entries: [], path: "", selected: [],
    view: "files", registering: false, busy: false, authError: "", toast: "", report: "", announcements: [], sharedFiles: [],
    uploading: false, uploadProgress: 0, sort: { field: "name", ascending: true }, movePickerOpen: false, movePickerPath: "", movePickerEntries: [], movePickerBusy: false,
    loginForm: { groupId: "", username: "", password: "" }, registerForm: { groupId: "", username: "", password: "", confirm: "" },
  }; },
  computed: {
    pathParts() { return this.path ? this.path.split("/") : []; }, movePickerParts() { return this.movePickerPath ? this.movePickerPath.split("/") : []; },
    currentAnnouncement() { return this.announcements[0] || null; },
    allSelected() { return this.entries.length > 0 && this.selected.length === this.entries.length; },
    sortedEntries() { const {field, ascending} = this.sort; const factor = ascending ? 1 : -1; return [...this.entries].sort((a,b) => { if (a.type !== b.type) return a.type === "folder" ? -1 : 1; let x = field === "modified" ? Date.parse(a.modified_at || 0) : field === "size" ? Number(a.size || a.total_size || 0) : String(a.name).localeCompare(String(b.name), "zh-CN", {numeric:true}); return x * factor; }); },
  },
  async mounted() { await this.loadGroups(); if (this.token) await this.restore(); },
  methods: {
    async request(path, options = {}) { const headers = {...(options.headers || {})}; if (this.token) headers.Authorization = `Bearer ${this.token}`; if (options.json !== undefined) { headers["Content-Type"] = "application/json"; options.body = JSON.stringify(options.json); } const response = await fetch(apiBase + "/api" + path, {...options, headers}); if (response.status === 401) this.logout(false); return response; },
    async jsonError(response, fallback) { try { return (await response.json()).detail || fallback; } catch (_) { return fallback; } },
    notify(message) { this.toast = message; window.clearTimeout(this.toastTimer); this.toastTimer = window.setTimeout(()=>this.toast="", 2800); },
    async loadGroups() { try { const r=await fetch(apiBase+"/api/auth/classes"); this.groups=(await r.json()).classes || []; } catch (_) { this.authError="无法连接服务，请稍后重试。"; } },
    async login() { this.authError=""; this.busy=true; try { const r=await this.request("/auth/login", {method:"POST",json:{class_id:Number(this.loginForm.groupId),username:this.loginForm.username,password:this.loginForm.password}}); if(!r.ok){this.authError=await this.jsonError(r,"登录失败。");return;} const data=await r.json(); this.token=data.token; this.user=data.user; localStorage.setItem("storage_token",this.token); await this.restore(); } catch (_) { this.authError="无法连接服务，请稍后重试。"; } finally {this.busy=false;} },
    async register() { this.authError=""; this.busy=true; try { const r=await this.request("/auth/register", {method:"POST",json:{class_id:Number(this.registerForm.groupId),username:this.registerForm.username,password:this.registerForm.password,confirm_password:this.registerForm.confirm}}); this.authError=await this.jsonError(r,r.ok?"注册申请已提交，请等待管理员审核。":"提交失败。"); if(r.ok){this.registerForm={groupId:"",username:"",password:"",confirm:""};this.registering=false;} } catch (_) {this.authError="无法连接服务，请稍后重试。";} finally {this.busy=false;} },
    async restore() { const r=await this.request("/auth/me"); if(!r.ok)return; this.user=(await r.json()).user; await Promise.all([this.openFiles(""), this.loadAnnouncements()]); },
    async logout(call=true) { if(call && this.token) await this.request("/auth/logout",{method:"POST"}); this.token="";this.user={};this.entries=[];this.selected=[];localStorage.removeItem("storage_token"); },
    async openFiles(path) { this.view="files";this.path=path;this.selected=[];await this.loadFiles(); },
    async loadFiles() { try { const r=await this.request("/files?path="+encodeURIComponent(this.path)); if(!r.ok){this.notify(await this.jsonError(r,"目录读取失败。"));return;} const data=await r.json();this.path=data.path||"";this.entries=data.entries||[];this.selected=[]; }catch(_){this.notify("目录读取失败。");} },
    toggleAll(e) {this.selected=e.target.checked?this.entries.map(x=>x.path):[];}, toggleSort(field){this.sort.field===field?this.sort.ascending=!this.sort.ascending:this.sort={field,ascending:true};}, sortMark(field){return this.sort.field===field?(this.sort.ascending?"↑":"↓"):"";},
    formatSize(bytes){const n=Number(bytes||0);if(!n)return "0 B";const u=["B","KB","MB","GB","TB"],i=Math.min(Math.floor(Math.log(n)/Math.log(1024)),u.length-1);return `${(n/1024**i).toFixed(i?1:0)} ${u[i]}`;}, formatDate(value){if(!value)return "--";const normalized=typeof value.replace==="function"?value.replace(" ","T"):value;const d=new Date(normalized||value);return Number.isNaN(d)?value:`${d.getFullYear()}/${String(d.getMonth()+1).padStart(2,"0")}/${String(d.getDate()).padStart(2,"0")} ${String(d.getHours()).padStart(2,"0")}:${String(d.getMinutes()).padStart(2,"0")}`;}, fileEmoji(name){const ext=(name.split(".").pop()||"").toLowerCase();if(["jpg","jpeg","png","gif","webp"].includes(ext))return "🖼️";if(["doc","docx"].includes(ext))return "📘";if(["xls","xlsx"].includes(ext))return "📗";if(["ppt","pptx"].includes(ext))return "📙";if(ext==="pdf")return "📕";if(["zip","rar","7z"].includes(ext))return "🗜️";return "📄";},
    async makeFolder(){const name=window.prompt("请输入新文件夹名称：");if(!name||!name.trim())return;const r=await this.request("/files/mkdir",{method:"POST",json:{path:this.path,name:name.trim()}});if(!r.ok)return this.notify(await this.jsonError(r,"创建失败。"));this.notify("文件夹已创建。");await this.loadFiles();},
    async upload(files, folder=false){const list=Array.from(files||[]);if(!list.length)return;this.uploading=true;this.uploadProgress=5;try{const form=new FormData();form.append("path",this.path);list.forEach(file=>{form.append("files",file);form.append("relative_paths",folder?(file.webkitRelativePath||file.name):file.name);});const headers={Authorization:`Bearer ${this.token}`};const r=await fetch(apiBase+"/api/files/upload",{method:"POST",headers,body:form});this.uploadProgress=100;if(!r.ok)return this.notify(await this.jsonError(r,"上传失败。"));this.notify("上传完成。");await this.loadFiles();await this.refreshUser();}catch(_){this.notify("上传失败。");}finally{this.uploading=false;this.$refs.files.value="";this.$refs.folder.value="";}},
    dropFiles(event){this.upload(event.dataTransfer.files);},
    async startDownload(paths){const r=await this.request("/files/download/prepare",{method:"POST",json:{paths,base:this.path}});if(!r.ok)return this.notify(await this.jsonError(r,"下载失败。"));const a=document.createElement("a");a.href=(await r.json()).url;a.click();this.notify("下载已开始。");}, downloadSelected(){this.startDownload(this.selected);},
    async rename(){const path=this.selected[0],current=this.entries.find(x=>x.path===path);const name=window.prompt("请输入新名称：",(current&&current.name)||"");if(!name||!name.trim())return;const r=await this.request("/files/rename",{method:"POST",json:{path,name:name.trim()}});if(!r.ok)return this.notify(await this.jsonError(r,"重命名失败。"));this.notify("重命名完成。");await this.loadFiles();},
    async openMovePicker(){this.movePickerOpen=true;await this.loadMovePicker("");}, async loadMovePicker(path){this.movePickerBusy=true;try{const r=await this.request("/files?path="+encodeURIComponent(path));if(!r.ok)return this.notify(await this.jsonError(r,"目录读取失败。"));const data=await r.json();this.movePickerPath=data.path||"";this.movePickerEntries=(data.entries||[]).filter(item=>item.type==="folder");}catch(_){this.notify("目录读取失败。");}finally{this.movePickerBusy=false;}}, async confirmMove(){const r=await this.request("/files/move",{method:"POST",json:{paths:this.selected,destination:this.movePickerPath}});if(!r.ok)return this.notify(await this.jsonError(r,"移动失败。"));this.movePickerOpen=false;this.notify("移动完成。");await this.loadFiles();},
    async remove(){if(!window.confirm(`确认删除选中的 ${this.selected.length} 项吗？文件将进入回收站。`))return;const r=await this.request("/files/batch-delete",{method:"POST",json:{paths:this.selected}});if(!r.ok)return this.notify(await this.jsonError(r,"删除失败。"));this.notify("已移入回收站。");await this.loadFiles();await this.refreshUser();},
    async refreshUser(){const r=await this.request("/auth/me");if(r.ok)this.user=(await r.json()).user;},
    async loadAnnouncements(){const r=await this.request("/auth/announcements");if(r.ok)this.announcements=(await r.json()).items||[];},
    async loadShared(){const r=await this.request("/auth/shared-files");if(!r.ok)return this.notify(await this.jsonError(r,"共享文件读取失败。"));this.sharedFiles=(await r.json()).items||[];},
    async openShared(){this.view="shared";await this.loadShared();},
    async downloadShared(item){const r=await this.request(`/auth/shared-files/${item.share_id}/download`);if(!r.ok)return this.notify(await this.jsonError(r,"下载失败。"));const a=document.createElement("a");a.href=URL.createObjectURL(await r.blob());a.download=item.type==="folder"?`${item.name}.zip`:item.name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);},
    async submitReport(){if(!this.report)return;this.busy=true;const r=await this.request("/auth/reports",{method:"POST",json:{content:this.report}});this.busy=false;if(!r.ok)return this.notify(await this.jsonError(r,"提交失败。"));this.report="";this.notify("报告已提交给管理员。");},
  },
}).mount("#app");
